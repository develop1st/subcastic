const root = document.getElementById('root');

root.innerHTML = `
  <h1>Subcastic</h1>
  <p>Frontend app scaffolded for segment-first playback UX.</p>
  <p>Next step: connect to <code>GET /stream/:user_id</code>.</p>
`;
